[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["chmod +x scripts/railway-build.sh && ./scripts/railway-build.sh"]

[start]
cmd = "node dist/index.js"
